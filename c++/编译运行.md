# GCC/G++ 编译器中指定库文件(LIB)、头文件(INCLUDE)

* [1. GCC/G++ 编译器中指定库文件(LIB)、头文件(INCLUDE)](https://itcn.blog/p/223075805.html#1_GCCG_LIBINCLUDE_14)
* * [1.1. 环境变量:](https://itcn.blog/p/223075805.html#11__25)
  * * [1.1.1. 编译时用到的环境变量:](https://itcn.blog/p/223075805.html#111__29)
    * [1.1.2. 运行时用到的环境变量:](https://itcn.blog/p/223075805.html#112__45)
  * [1.2. 示例:](https://itcn.blog/p/223075805.html#12__53)
  * [1.3. 运行](https://itcn.blog/p/223075805.html#13__83)
  * [1.4. /etc/ld.so.conf 文件](https://itcn.blog/p/223075805.html#14_etcldsoconf__94)

# 1. GCC/G++ 编译器中指定库文件(LIB)、头文件(INCLUDE)

库文件在连接 (静态库和共享库) 和运行 (仅限共享库的程序, 静态库会和可执行编译到一起) 时被使用, 其搜索路径是在系统中进行设置的。一般 Linux 系统把 `/lib` 和 `/usr/lib` 两个目录作为默认的库搜索路径, 所以使用这两个目录中的库时不需要进行设置搜索路径即可直接使用。对于处于默认库搜索路径之外的库, 需要将库的位置添加到库的搜索路径之中。

设置库文件的搜索路径总的来说有以下几种:

* `LIBRARY_PATH`、`LD_LIBRARY_PATH` 等环境变量: 指定连接、运行时库文件路径;
* `/etc/ld.so.conf` 文件: 添加链接时库文件的搜索路径, 运行时还需要使用 `ldconfig` 命令将路径刷新到 `ld.so.cache` 中;
* g++/gcc 参数 `-L`、`-l`、`-I`: 指定链接时库文件的路径、名字和头文件, 运行时还需要使用环境变量或者在文件 `/etc/ld.so.conf` 中指定(或者放到默认 `/lib` 路径), 然后 `ldconfig`;

接下来我们分连接、运行不同阶段来分别解读一下。见: 动态链接、静态链接. note

## 1.1. 环境变量:

在 `/etc/profile` 中添加如下环境变量。

### 1.1.1. 编译时用到的环境变量:

```shell
#gcc 找到头文件的路径
C_INCLUDE_PATH=/usr/include/libxml2:/MyLib
export C_INCLUDE_PATH

#g++ 找到头文件的路径
CPLUS_INCLUDE_PATH=$CPLUS_INCLUDE_PATH:/usr/include/libxml2:/MyLib
export CPLUS_INCLUDE_PATH
 
#gcc 和 g++ 在编译的链接阶段查找库文件动态库、静态库的路径
LIBRARY_PATH=$LIBRARY_PATH:/MyLib
export LIBRARY_PATH
```

### 1.1.2. 运行时用到的环境变量:

```shell
# 程序运行时查找 ku 文件的路径
LD_LIBRARY_PATH=$LD_LIBRARY_PATH:/MyLib
export LD_LIBRARY_PATH
```

### 1.1.3. 示例:

一个头文件 `my_so_test.h` 和两个源文件 `test1.cpp`、`test2.cpp`, 将他们制作成一个名为 `libmytest.so` 的动态链接库文件:

1.先通过 `$ g++ -o libmytest.so -shared -fPIC test1.cpp test2.cpp` 创建动态链接库;

保存目录为：/home/xxx/test/lib

2.编写一个 `main.cpp` 调用动态连接库:

```C
#include "my_so.h"
int main()
{
  test1();
  test2();
  
  return 0;
}
```

编译、连接:

解决方案: `vim /etc/profile`, 添加如下, 然后 `source /etc/profile`:

```shell
LIBRARY_PATH=$LD_LIBRARY_PATH:/home/xxx/test/lib
export LIBRARY_PATH
```

然后再编译、连接: `$ g++ -o run main.cpp -lmytest` 没问题了。

### 1.1.4. 运行

`vim /etc/profile`, 添加如下, 然后 `source /etc/profile`

```shell
LD_LIBRARY_PATH=$LD_LIBRARY_PATH:/home/xxx/test/lib
export LD_LIBRARY_PATH
```

然后再运行 `./run` 没问题了。

当然可以不指定LIBRARY_PATH，直接编译时指定LD_LIBRARY_PATH也可以。

## 1.2 修改 /etc/ld.so.conf 文件:

将库文件的路径都加入到 `/etc/ld.so.conf` 中是明智的选择, 添加方法也很简单, 一行一个, 例如:

```shell
/usr/X11R6/lib
/usr/local/lib
/opt/lib
```

添加完成后只是链接时不会报错, 执行前还需要使用 `ldconfig` 命令刷新到 `ld.so.cache` 中。

示例: 还是上面的例子

1.连接:

将动态库拷贝到系统默认的库路径中, 即可编译链接通过。或者在 `/etc/ld.so.conf` 中加上一行 `/home/xxx/lib`

2.运行:

解决方案: 执行 `ldconfig`, 之后即可运行成功。

## 1.3 gcc/g++ 参数:

通上面的例子看一下 `-L`、`-l`、`-I`(指定头文件的)参数。

1)编译、连接:

```shell
$ g++ -o run main.cpp -L./ -lmytest
```

说明: 通过 `-L` 参数指定库文件的地址; `-l` 指定动态库的名字;-I指定头文件地址。

2)运行前指定动态库路径:

通过 `LD_LIBRARY_PATH` 环境变量, 或者在文件 `/etc/ld.so.conf` 中指定库文件路径(或者将库文件放到默认 `/lib` 路径), 然后 `ldconfig` 刷新 `ld.so.cache`。

# 2 查看编译器默认搜索路径

默认库文件路径、头文件路径

1、查看默认库文件路径:

```shell
gcc --print-search-dir
g++ --print-search-dir
```

2、查看默认头文件 (include) 路径:

```shell
c:  gcc -xc -E -v -

c++: gcc -xc++ -E -v -

```

3   编译指定文件时，添加-v参数也可以查看include的搜索路径

`g++ head.cpp -v. ## -v的意思是verbose，详细冗余的信息 `

# 3 编译和链接时搜索库顺序

**ld.so 动态共享库搜索顺序**

1、ELF可执行文件中动态段DT_RPATH指定；gcc加入链接参数“-Wl,-rpath”指定动态库搜索路径；
 2、环境变量LD_LIBRARY_PATH指定路径；
 3、/etc/ld.so.cache中缓存的动态库路径。可以通过修改配置文件/etc/ld.so.conf 增删路径（修改后需要运行ldconfig命令）；
 4、默认的 /lib/;
 5、默认的 /usr/lib/

**Linux 下动态链接库搜索路径问题**

Linux动态链接库的搜索路径按优先级排序为：

1.编译目标代码时指定的动态库搜索路径；

在编译时通过gcc 的参数”-Wl,-rpath,”指定。当指定多个动态库搜索路径时，路径之间用冒号”：”分隔。

2.环境变量LD_LIBRARY_PATH指定的动态库搜索路径；

3.配置文件/etc/ld.so.conf中指定的动态库搜索路径；

/etc/ld.so.conf的第一行有个引用命令：include ld.so.conf.d/*.conf

因此，最优雅的方式是在ld.so.conf.d目录下创建一个你的程序依赖的配置文件，配置文件内容为程序依赖的动态链接库的路径，一个路径一行。

添加完配置文件后执行ldconfig使其生效。

4.默认的动态库搜索路径/lib；

5.默认的动态库搜索路径/usr/lib；

1、可以用 LD_LIBRARY_PATH 环境变量指定，这个类似于 PATH 机制，比较直观，而且，可以放到 bashrc 中固化下来，也可以放到自己的 .bashrc 中只对本用户起作用；

2、如果启用了 ld.so.cache 的话，系统会在 /etc/ld.so.cache 中存储所有可引用的动态链接库。这个文件的内容可以通过 /etc/ld.so.conf 来指定；这个是比较固定的机制，对全局所有用户都有影响；不过更改设置后需要 root 调用 ldconfig 来刷新一下。

3、默认的标准库路径，这个似乎不用设置就可以。包括 /lib 和 /usr/lib。当然，如果是64位系统，还包括 /lib64 和 /usr/lib64。

4、其它情况，如果只想对某一个特定的应用程序起作用的话，可以在编译时指定搜索路径。gcc 的 -Wl 和 -rpath 参数。

# **4 与动态链接库相关命令**

 （1）ld 是gcc的链接程序。
 （2）ldd是查看可执行文件中所依赖的库的程序，比如想查main程序用到了那些动态库，可以直接  ldd main
 （3）ldconfig用来更新文件/etc/ld.so.conf的修改生效。
 （4）nm用来查看.so库中的函数名字，标记是T的就是动态库里面生成的名字。如：nm /lib/libc*.so

# 5 LD_PRELOAD之偷梁换柱

[参考](https://www.cnblogs.com/net66/p/5609026.html)
